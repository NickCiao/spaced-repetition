{
  "name": "spaced-repetition",
  "install": "npm install && ([ -f .dev.vars ] || printf 'SR_TOKEN=devtoken\\n' > .dev.vars) && npm run migrate:local",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ],
  "ports": [8787]
}
